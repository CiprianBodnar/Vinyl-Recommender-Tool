import React, {Component} from 'react';

export default class Footer extends Component{
    render(){
        const footerS={
            textAlign: 'center',
        };
        return(
            <footer style={footerS}>footer</footer>
        )
    }
}
